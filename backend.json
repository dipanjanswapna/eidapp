{
  "entities": {
    "nglUser": {
      "title": "NGL User",
      "description": "An NGL user profile.",
      "type": "object",
      "properties": {
        "username": { "type": "string" },
        "pin": { "type": "string" },
        "name": { "type": "string" },
        "gender": { "type": "string", "enum": ["male", "female", "other"] },
        "profession": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["username", "pin", "name", "gender", "profession", "createdAt"]
    },
    "nglMessage": {
      "title": "NGL Message",
      "description": "An anonymous message sent to an NGL user.",
      "type": "object",
      "properties": {
        "receiverUsername": { "type": "string" },
        "senderTag": { "type": "string" },
        "message": { "type": "string" },
        "isReplied": { "type": "boolean" },
        "reply": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["receiverUsername", "senderTag", "message", "isReplied", "createdAt"]
    },
    "eidCard": {
      "title": "Eid Card",
      "description": "A digital Eid card with a personal message and payment options.",
      "type": "object",
      "properties": {
        "recipientName": { "type": "string" },
        "message": { "type": "string" },
        "theme": { "type": "string", "enum": ["royal-blue", "bright-red", "golden-yellow"] },
        "bkashNumber": { "type": "string" },
        "nagadNumber": { "type": "string" },
        "rocketNumber": { "type": "string" },
        "targetAmount": { "type": "number" },
        "isPaid": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["recipientName", "message", "theme", "createdAt"]
    },
    "iftarSpot": {
      "title": "Iftar/Sehri Spot",
      "description": "A user-submitted spot for Iftar or Sehri.",
      "type": "object",
      "properties": {
        "masjidName": { "type": "string" },
        "area": { "type": "string" },
        "foodType": { "type": "string", "enum": ["kacchi-biryani", "tehari", "khichuri", "polao-korma", "beef-roti", "chicken-biryani", "mutton", "haleem-jilapi", "mixed-iftar", "sehri-thali", "others"] },
        "otherFoodTypeName": { "type": "string" },
        "latitude": { "type": "number" },
        "longitude": { "type": "number" },
        "likes": { "type": "number" },
        "dislikes": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["masjidName", "area", "foodType", "latitude", "longitude", "likes", "dislikes", "createdAt"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/ngl_users/{username}": {
        "schema": { "$ref": "#/entities/nglUser" },
        "description": "NGL user profiles, with the username as the document ID."
    },
    "/ngl_messages/{messageId}": {
        "schema": { "$ref": "#/entities/nglMessage" },
        "description": "All NGL messages."
    },
    "/eid_cards/{cardId}": {
        "schema": { "$ref": "#/entities/eidCard" },
        "description": "User-generated digital Eid cards."
    },
    "/iftar_spots/{spotId}": {
      "schema": { "$ref": "#/entities/iftarSpot" },
      "description": "User-submitted Iftar and Sehri spots, valid for 24 hours."
    }
  }
}
